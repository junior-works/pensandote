# --- Pensándote ---

# config.js SÍ se commitea desde la v0.3 (deploy a GitHub Pages).
# SUPABASE_URL + anon/publishable key son PÚBLICAS por diseño: el control
# real lo hace RLS. La service_role NUNCA va al front. config.example.js
# queda como referencia.

# OS / editor
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp
*~

# Dependencias futuras (por ahora no usamos npm, pero por las dudas)
node_modules/

# Build / caches (si en el futuro agregamos build step)
dist/
.cache/
*.log

# Supabase CLI local (si más adelante linkeamos)
.supabase/
supabase/.branches
supabase/.temp

# Scripts de diagnóstico locales (usan service_role o cosas sensibles)
_test_*.mjs
_test_*.js
_diag_*.mjs
_diag_*.js
